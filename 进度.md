# 三国杀模组 · 项目进度档案

> 用途:跨对话接力。开新对话时先读本文件,再继续开发。
> 最后更新:2026-08-07(进度快照)

## 一、项目基础信息

| 项 | 值 |
|---|---|
| 项目路径 | `C:\Users\Administrator\dev\MCsanguosha` |
| modid / 版本 | `sanguosha` / gradle 1.3.0(分发备份另命名 sanguosha1.4.0.jar) |
| 技术栈 | NeoForge 1.21.1(NeoForge 21.1.234)、Java 21 |
| 部署目标 | PCL 实例「三国杀」→ `C:\Users\Administrator\Desktop\.minecraft\versions\三国杀\mods`(版本隔离;PCL 本体 Downloads\PCL) |
| 网络 | 构建前 `$env:JAVA_TOOL_OPTIONS="-Dhttp.proxyHost=127.0.0.1 -Dhttp.proxyPort=7890 -Dhttps.proxyHost=127.0.0.1 -Dhttps.proxyPort=7890"`(Mojang 经 nonProxyHosts 直连) |
| 构建产物 | `build\libs\sanguosha-1.3.0.jar`(约 186MB,含 63 武将+2 张新贴图) |
| 分发位置 | 最新 `Videos\Captures\方块杀\`;旧版归档 `方块杀旧版\`;整合包 `Downloads\MCsanguosha-1.3.0-modpack.zip` |

## 二、游戏模式

**2v2 欢乐成双**(服务器权威状态机 `SanguoshaGame`,约 1100 行):
- 座位 0~3,0/3 红方,1/2 蓝方;同队相邻距离视为 1
- 状态机:`WAITING → HERO_SELECT → RUNNING → FINISHED`
- 机制:响应队列(闪/杀)、选择队列(花色/改判/流离/观星)、弃牌阶段、出牌动画广播(animSeq 防重播)
- 指令 `/sanguosha`:join / start / leave / reset / status / card / deck / table / clear

## 三、卡牌(标准版 108 张)

- 构成:基本牌 53(杀30/闪15/桃8)+ 锦囊 35 + 装备 20,`Cards.java` 静态牌表
- ⚠️ 花色点数是「通行标准版近似值」,注释标注待贴图到位后校对
- 模型:`models/item/card/1~71.json`(71 个牌面模型)
- 贴图:`textures/card/*.png`(标准牌 + 军争牌已入资源:火杀/雷杀/铁索/火攻/闪电等)
- 效果实现(`EffectRegistry`):
  - 基本牌:slash / fire_slash / thunder_slash / jink / peach / analeptic ✅
  - 锦囊:duel / dismantlement / snatch / exnihilo / savage / archery / amazing / bountiful / nullification / collateral / indulgence / bingliangcunduan / iron_chain / huogong / lightning ✅
  - 装备:crossbow(有逻辑)✅;gudingdao / zhuqueyushan / baiyinshizi 仅空 Handler 占位 ⚠️

## 四、武将(27 名)

- 标准版 25 将 + 袁术、华雄;魏7 蜀7 吴8 群3 扩展2(`Heroes.java`)
- 贴图 `textures/hero/*.png`:27 张武将图**已齐**
- **第一批已实现(被动/自动)**:咆哮、马术、英姿、裸衣、集智、天妒、遗计、奸雄、反馈、刚烈、克己、连营(仅注册)、谦逊、闭月、耀武、空城、洛神、庸肆
- **交互技能**:网络层已有 `LIJIAN / RENDE / FANJIAN / CONVERT_PLAY / SKILL / CHOICE / RECAST` 的 ActionPacket 分支(`ServerPayloadHandler` 有 useLijian/useRende/useFanjian/playCardConverted),但 `StandardSkills` 注释仍标「待实现」→ ⚠️ 逻辑可能半成品,需逐个核对
- **明确待实现**:青囊、武圣、龙胆、倾国、奇袭、国色、流离、仁德(交互)、制衡、苦肉、离间(交互)、反间(交互)、铁骑、无双、鬼才、观星、枭姬(装备区监听)、结姻、急救(濒死救援)
- 主公技(护驾/激将/救援/伪帝):仅注册名称,2v2 模式不生效

## 五、网络

- `Networking` + `ActionPacket`(动作)+ `GameSyncPacket`(状态同步)+ 双端 Handler
- Action 全集:JOIN / START / SORT_HAND / LEAVE / RESET / SELECT_HERO / PLAY_CARD / PASS / DISCARD / RESPOND_YES / RESPOND_NO / CONVERT_PLAY / SKILL / RECAST / LIJIAN / RENDE / CHOICE / FANJIAN / HP_UP / HP_DOWN / PLACE_CARD / DROP_CARD / DEMOLISH / CLEAR_CARDS
- `syncAll` 广播全量状态;服务器端按玩家 UUID 处理动作

## 六、UI / 渲染

- 界面:大厅 LobbyScreen、选将 HeroSelectScreen、主局 SanguoshaScreen、武将详情 HeroDetailScreen、玩家详情 PlayerDetailScreen、选择 ChoiceScreen、拆迁 DemolishScreen
- HUD:`SanguoshaHud` 右上血量 + 炉石式手牌(H 开关、J/K 调血、滚轮/数字选牌),`ClientHudText`
- 渲染:`CardEntityRenderer`(实体卡牌)、`CardItemRenderer` + `CardItemExtensions`(物品渲染,1.21 API 坑见技能 mc-custom-texture-rendering)
- 键位 `ModKeybinds`:HP_UP / HP_DOWN / TOGGLE_UI / PLACE_CARD / DROP_CARD / CLEAR_CARDS

## 七、实体 / 物品 / 方块

- `CardEntity` 实体卡牌(可放置桌上);`CardItem` 物品卡牌(CardData 注册,按牌自定义模型)
- 卡盒:`DeckBoxItem`(牌堆盒)、`HeroDeckBoxItem`(武将卡盒)
- 方块:`CardBoxBlock`、`hero_deck_box_block` + 贴图(箱顶/侧面)
- `CardPlaceEvents`:右键实体放置卡牌

## 八、音效(29 个 OGG,MC 不认 MP3)

- 通用事件:card_use / card_draw / damage / death / win / button / judge
- 武将语音:slash / jink / peach / duel / archery / snatch / savage / exnihilo / bountiful / nullification / iron_chain / combo1-3 等(分男/女声)
- 播放链路:`ModSounds` 注册 → sounds.json 骨架 → 客户端按 `lastLog` 关键词自动播(`AudioManager`)

## 九、✅ 当前状态(2026-08-03 已打包部署)

**已编译打包并部署到 PCL「打牌来」实例(12:46 构建,`sanguosha-1.0.0.jar`,约 25MB)。**
- 本次修复:`ClientHudText.java` HudStand 构造器 `EntityType<?>` → `EntityType<? extends ArmorStand>`(用户改出的类型不匹配,导致编译失败)
- 本次代理坑:NeoGradle 下载 Mojang 资源走 FlClash 代理失败 → 已在项目 gradle.properties 加 `systemProp.http.nonProxyHosts=*.mojang.com|*.minecraft.net|*.mojang.net|localhost|127.*|[::1]`,Mojang 直连、neoforged 走代理
- ⚠️ 旧版 `sanguosha-1.1.0.jar` 已重命名禁用(`.jar.disabled`),避免双 jar 冲突
- 剩余 8 个编译警告:`@EventBusSubscriber` 的 `bus()` 过时 API(待删除标记),不影响运行,后续可清理
- **12:56 二次修复(漂浮文字)**:Marker ArmorStand 碰撞箱为 0,名字标签渲染位置是实体上方 **0.5 格**(普通模式是 bbHeight+0.5≈2.475),原 -2.475 补偿导致文字在地下;已改 -0.5,`setInvisible` 移到最后。坑:`ArmorStand.setMarker()` 是 **private**,只能通过子类调 `readAdditionalSaveData`(NBT Marker=true)设置
- **13:06 三次修复(彻底换方案)**:ArmorStand NBT hack 导致 Marker 时灵时不灵,实体碰撞箱挡住牌盒右键(摸不了牌)。已弃用 ArmorStand,**改用 TextDisplay 实体**(`EntityType.TEXT_DISPLAY`):原生无碰撞箱(不挡右键)、不渲染实体本体(无盔甲架问题)、文字以实体坐标为中心(无需高度补偿)。1.21.1 坑:`TextDisplay.setText()` 和 `DATA_TEXT_ID` 均为 **private**,只能经子类 `readAdditionalSaveData` + NBT(`text`=Component JSON、`billboard`=center)设置;`Component.Serializer.toJson` 需传 `level.registryAccess()`。已验证(反编译 6 项全 PASS)
- **13:12 HUD 按键提示动态化**:左侧提示(原硬编码 "J/K: 血量±1" 等)改为从 `KeyMapping.getKey().getDisplayName()` 动态读取(`keyName()` 辅助方法);SanguoshaClientEvents 的"按 G 打开牌桌"同步动态化(OPEN_TABLE)。玩家改绑后提示自动跟随;未绑定显示"未绑定"。已验证(4 项全 PASS)
- **13:28 牌盒/将盒粒子效果**:破坏时 `CardBoxBlock.spawnDestroyParticles(Level,Player,BlockPos,BlockState)` 重写 → 原版碎屑 + `ServerLevel.sendParticles` 广播 24 粒对应颜色 dust(牌盒蓝 0.35/0.55/0.95、将盒红 0.95/0.35/0.35);踩踏时新类 `BlockParticleEffects`(客户端 `ClientTickEvent.Post`,脚下方块为牌盒则每 8 tick 冒一粒 dust)。零贴图依赖。坑:NeoForge 1.21 已移除 `AddBlockDestroyEffectsEvent`,破坏粒子必须走方块 `spawnDestroyParticles` 重写。已验证(字节码检查全 PASS)
- **13:48 粒子改回原版风格 + 紫黑块根因**:用户要的是原版方块纹理碎片粒子而非彩色 dust,且方块/粒子显示紫黑块。**根因:方块模型 `"parent": "block/block"` 是 Forge 时代特殊父模型,NeoForge 1.21 不存在 → 模型解析失败 → 方块与破坏粒子全紫黑**。修复:① 模型去掉 parent,改自包含元素模型并加 `"particle"` 纹理字段(破坏粒子取方块贴图 box_top/herobox_top);② 破坏粒子删掉自定义重写,恢复原版默认(levelEvent 2001);③ 踩踏粒子改 `ParticleTypes.BLOCK`(`BlockParticleOption`)原版方块粒子。**教训:NeoForge 1.21 自定义元素模型不要用 block/block parent**。已验证(jar 内模型 JSON + 字节码检查)
- **14:07 丢牌(Q 键)修复**:根因是 **ItemStack 引用 bug**——`get(slot)` 拿到的是物品栏里的对象引用,`removeItem(slot,1)` 会把该对象数量归零,再 `new ItemEntity(..., card)` 传同一个(已空)对象 → 凋落物内物品为空 → 原版 tick 立即销毁 → "牌消失、捡不到"。修复:先 `.copy()` 再删,并改用**最原版方式 `player.drop(card, false)`**(原版 Q 丢物品,自动生成凋落物在玩家面前带物理)。教训:传 ItemStack 给实体/掉落物前务必 copy。已验证(字节码 4 项全 PASS;注意生产 jar 是 reobf 混淆,MC 方法名验证要在 build/classes 未混淆产物上做)
- **14:21 HUD 输入拦截 + 花色增强**:① HUD 显示时滚轮/数字键不再切换原版物品栏——滚轮 `MouseScrollingEvent.setCanceled(true)`(可取消);数字键 `InputEvent.Key` **不可取消**(NeoForge 1.21 无 ICancellableEvent),改用 `ClientTickEvent.Pre` 在 `Minecraft.handleKeybinds` 前**抢先 consumeClick** `options.keyHotbarSlots[i]`。② 手牌花色显示放大 1.8 倍 + 四方向黑描边 + 亮色(红桃/方块 0xFFFF5555,黑花白字)。已验证(5 项全 PASS)

## 十、常规工作流

1. 用户在 VS Code 改源码(有时带 AI 助手 → 可能编译失败)
2. 我:`gradlew compileJava` 修错误 → `gradlew build` 打包
3. 复制 jar 到 `D:\PCL2\.minecraft\versions\1.21.1-打牌来\mods\`
4. 素材(贴图/音效)用户放 `C:\Users\BigMer\Downloads`,处理时验证 PNG 签名(89 50 4E 47);音频用 ffmpeg 转 OGG
5. 排障先查技能 `mc-custom-texture-rendering`(textures/item/ 根因 + 1.21 API 坑)

- **14:30 花色显示微调 + 放置方向反转**:① HUD 手牌花色改回原字号(去掉 1.8 倍 scale),保留黑描边 + 亮色(红桃/方块 0xFFFF5555、黑花白字);② 地上牌实体(CardEntityRenderer)的花色同步高亮(原为暗色无描边,现加四方向黑描边 + 亮色);③ R 键放置方向 180° 反转(placeSelectedCard rot = getYRot() + 180)。已验证(字节码:无 1.8f 常量、+180.0f、drawInBatch x5 + 黑色描边)。注意:本机 read_file/patch/write_file 工具已不可用,改文件统一用 PowerShell [System.IO.File]::ReadAllText/WriteAllText(UTF-8 无 BOM);javap 调用要用 PATH 方式(避免 & 误判为后台符);中文进命令行易乱码,优先 \uXXXX 转义
- **14:34 实体牌摩尔纹修复**:① 花色文字轻微放大(scale 0.018->0.022)+ 抬离牌面(z 0.011->0.02)+ DisplayMode.NORMAL->POLYGON_OFFSET(多边形深度偏移,防文字与牌面 z-fighting 产生波纹);② 牌面渲染 y 加按实体 id 的微偏移( .03 + (id%10)*0.003),叠放的两张牌深度错开,消除 z-fighting 闪烁。已验证(字节码 3 项)。经验:世界空间小字号文字 + 半透明 + 同深度叠加 = 摩尔纹,解法=放大字号/深度偏移/POLYGON_OFFSET
- **14:38 实体牌花色摩尔纹(二次修复)**:牌间叠放修复后花色文字仍有摩尔纹——根因是**5 次 drawInBatch 描边叠加**(4 向黑边+主体),5 层半透明文字在 3D 小字号下混合产生波纹。修复:改为**单次 drawInBatch + dropShadow=true**(原版物品名标签同款),字号 0.022->0.026。已验证(字节码:drawInBatch 仅 1 次、0.026f)。经验:3D 世界空间文字不要用多次偏移描边,用内置 dropShadow 或 drawInBatch8xOutline
- **14:47 牌盒物品使用提示**:DECK_BOX/HERO_DECK_BOX(牌盒/将盒方块物品)改为匿名 BlockItem 子类,override appendHoverText 添加描述「建议手持木棍或不能放置的东西游玩」(淡黄 0xFFFFE0B0;中文用 \\uXXXX 转义避免编码坑)。已验证(匿名类 ModItems/2 含 appendHoverText + 颜色常量)。经验:匿名内部类编译为独立 class(类名/),javap 验证要对匿名类文件而非宿主类;0xFFFFE0B0 有符号十进制为 -8016
- **15:56 牌盒/将盒剩余查看+拿取 UI**:蹲下(Shift)右键桌面牌盒/将盒实体 → 服务端 ServerPayloadHandler.sendRemain 发送剩余列表 → 客户端 RemainBoxScreen(自定义 UI:标题/列表/滚轮滚动/点击拿取)。新增 RemainSyncPacket(S2C,type+names)、ActionPacket.REMAIN_TAKE(C2S 拿取)、CardDeck/HeroDeckBox 加 
emainingList()+	ake(pos)。坑:record 组件名不能叫 	ype(与 CustomPacketPayload.type() 冲突,编译报'存取方法无效'),改名 boxType。已验证(字节码 5 项 + 注册确认;jar 15:56:24 部署)
- **16:04 新武将入将盒(仅数据+贴图,技能占位)**:① 奶龙(Faction.SHEN 神,4 血,技能 juhua 巨化/zhenglong 争龙);② 张雪峰(Faction.LAO 牢,5 血,技能 qiaolezi 巧乐兹/zuichunfazi 嘴唇发紫/lajinmiwu 拉进迷雾)。Faction 新增 SHEN(神 0xC9A6FF)/LAO(牢 0x9E9E9E);StandardSkills 注册 5 个占位技能(仅名称+描述,无逻辑,标记'待实现');贴图 textures/hero/nailong.png、zhangxuefeng.png(用户 Downloads 两张图 ffmpeg 缩放 256x256)。坑:替换多行代码块时 register(new Skill() 空壳残留导致匿名类嵌套错乱;技能 id 字符串在匿名类(StandardSkills.class)不在宿主类。jar 16:04:47 部署
- **16:11 新武将手持贴图+清晰度修复**:① 手持贴图不对根因:CardItemRenderer.renderByItem 未处理'武将:'前缀,keyOf 返回 back 显示牌背;已加武将分支(手持/HUD 用 textures/item/hero_<id>.png,与现有 27 将一致;实体卡牌用 textures/hero/<id>.png)。② 糊的根因:611x851 竖版图被强制压成方形 256x256(变形+降分辨率);重新用 ffmpeg 保持比例缩放到高 512(张雪峰 368x512、奶龙 372x512),hero 和 item 两处各放一份。坑:javap 中字符串拼接常量藏在 makeConcatWithConstants 引导方法里,直接 grep 'hero_' 匹配不到,要看 invokedynamic。已验证(字节码 startsWith/substring 分支 + jar 内 4 张贴图;jar 16:11:19 部署)
- **16:16 武将手持贴图真根因修复**:前次修复(BER)无效——**1.21 手持渲染不走 BER,走物品模型 custom_model_data**。真根因:CardModelIds.HERO_KEYS 只有 27 将,heroIdOf(nailong) 返回默认 45 -> custom_model_data=45 -> card/45.json -> 曹操贴图!两位新武将全显示曹操。修复:① HERO_KEYS 追加 nailong/zhangxuefeng(45+27=72、45+28=73);② 生成 models/item/card/72.json(hero_nailong)、73.json(hero_zhangxuefeng);③ card.json overrides 加 72/73。教训:加武将必须同步 HERO_KEYS+模型文件+overrides 三处(顺序 byte 级一致);新武将默认落到第一个武将(45)是本 bug 直接原因。已验证(jar 内模型链 4 项;jar 16:16:26 部署)
- **16:22 删除实体牌盒死代码**:DeckBoxItem/HeroDeckBoxItem(右键放置 CardEntity 牌盒/将盒的未注册物品类)完全孤立(仅互相引用,未注册未调用),已删除。保留:HeroDeckBox(武将牌堆静态类,在用于发将/剩余列表)、CardEntity(桌面卡牌实体,在线下玩法在用)、CardPlaceEvents(实体交互,普通牌翻转/捡起)。验证:引用扫描 0 外部引用 + jar 内无残留 class。方块牌盒(CardBoxBlock)是唯一牌盒入口。jar 16:22:29 部署
- **16:35 每牌盒独立牌堆+空堆停取+洗牌UI**:新建 LocalCardDeck/LocalHeroDeck(实例牌堆:shuffle/next(空返回 null)/take/remainingList/remaining)和 BoxDeckManager(按 BlockPos 两套 Map 管理,computeIfAbsent 惰性建堆)。CardBoxBlock 摸牌/发将/查看/剩余全部改走 BoxDeckManager(方块位置),空堆提示'牌盒已空/将盒已空';CardPlaceEvents(实体)用 disp.blockPosition() 同机制。RemainSyncPacket 加 posX/Y/Z;ActionPacket 加 REMAIN_SHUFFLE;ServerPayloadHandler:sendRemain(player,pos,type)、remainTake/remainShuffle 解析 'x,y,z,type' 编码(heroId 字段复用);RemainBoxScreen 加'洗牌'按钮(底部)+空堆提示'点击洗牌重新开局'+boxKey 编码。已知限制:牌堆存服务端内存(重启/换世界重置);方块破坏不清理 Map(同位置重放会继承旧牌堆)。已验证 8 项(jar 16:35:00 部署)
- **17:03 剩余 UI 搜索功能(中文+拼音)**:RemainBoxScreen 加 EditBox 搜索框(顶部标题下,提示'搜索(拼音/中文)',最大 16 字符,ESC 关闭优先);visibleNames() 实时过滤(中文包含 + 拼音包含),无结果提示'无匹配结果';列表/点击/滚动全部基于过滤结果。新建 PinyinUtil(43 牌名 + 29 武将名 -> 小写拼音映射,名字级,toPinyin 自动去花色/点数前缀,未知字符原样小写)。坑:1.21 EditBox 没有 tick() 方法(编译报错),删掉 tick 覆盖不影响输入。已验证 4 项(jar 17:03:46 部署)
- **17:07 崩溃修复(搜索框 font null)**:latest.log 定位:RemainBoxScreen 构造里 new EditBox(this.font, ...) 时 Screen.font 还是 null(setScreen 才赋值)-> 渲染时 EditBox 内 this.font NPE -> 'Rendering screen' 崩溃。修复:EditBox 移到 init() 创建(font 可用)。日志另见 Display entityNot a string ERROR(摸牌 TextDisplay,待观察是否影响漂浮文字)。jar 17:07:38 部署
- **17:21 添加卡牌快捷工具 tools/add_card.ps1**(实测通过):.\tools\add_card.ps1 -Name 牌名 -Key 贴图key -Category basic/trick/equip -Suit spade/heart/club/diamond -Rank 1-13 [-Effect id] [-Texture 贴图.png] [-Build]。自动完成:① Cards.java static 块末尾加 add(锚点 all() 方法前);② CardModelIds:keyOf case + EXTRA_KEYS 数组 + idOf 扩展查询;③ models/item/card/<N>.json 生成;④ card.json overrides 追加(正则匹配最后一个 override 行);⑤ 贴图复制到 textures/card+item。新牌 custom_model_data 从 100 起(1-44 普通牌,45-73 武将,74-99 预留)。坑:PS5.1 读 .ps1 按 ANSI 中文乱码→脚本必须 UTF-8 带 BOM;PowerShell 变量大小写不敏感( 覆盖 );Cards.java static 块在 add 方法之后;card.json 结尾 Replace 受换行符影响。实测:加测试牌→BUILD SUCCESSFUL→恢复备份。
- **17:35 添加武将工具 tools/add_hero.ps1**(实测通过):参数 -Name/-Id/-Faction(wei shu wu qun shen lao)/-Hp/-Texture/-Build。自动:Heroes.java(nameToId+add,无技能)、HERO_KEYS 追加、card/序号.json、card.json overrides、贴图 hero+item 两处;序号 N=45+heroCount-1 自动接续(实测新武将=74)。坑:正则须含捕获组(无组时 Groups[1] 恒空致 N=45 误覆盖曹操模型);追加元素引号顺序须先闭合旧元素再逗号再开引号;正则替换串组引用用单引号+变量拼接。实测加测试武将 BUILD SUCCESSFUL 后恢复。tools 现有:add_card.ps1(牌)+add_hero.ps1(武将)
- **19:54 用 add_hero.ps1 导入界徐盛**(首次实战):截图 JPEG(429x619)先 ffmpeg 转 PNG(354x512,scale=-2:512)→ add_hero.ps1 -Name 界徐盛 -Id jiexusheng -Faction wu -Hp 4 -Texture -Build → 6 项验证全过,jar 19:54:39 部署。注:脚本要求 PNG,JPEG 需先转换;武将序号接续到 74。
- **20:24 将盒搜索两 bug 修复**:① 搜不到界徐盛:PinyinUtil 武将拼音表是写死 29 将,新武将无拼音;改为 toPinyin 兜底查 Heroes.nameToId(需改 public),动态支持所有武将含未来新增。② 搜索后拿错牌(严重):UI 点击发送过滤后列表索引,服务端按完整牌堆取 → 错位;改 visibleNames→visibleIndexes(返回原始索引),渲染用 names.get(原索引),点击发送原索引。已验证 3 项,jar 20:24:11 部署。
- **21:43 牌盒掉落 + 牌布(4x3)**:① 牌盒/将盒可挖掘掉落(数据驱动 loot table:loot_table/blocks/deck_box_block.json、hero_deck_box_block.json,掉对应 BlockItem);坑:getDrops override 签名 1.21 不匹配,改 loot table;loot 文件名必须=方块注册名(card_mat 不是 card_mat_block)。② 牌布 CardMatBlock(mat_x 0-3/mat_y 0-2 分片属性,onPlace 按 floorMod(pos.x,4)/floorMod(pos.z,3) 设分片;1px 薄片地毯形状),材质 400x300 整图 textures/block/card_mat.png,12 个模型 card_mat_xy.json 各用 uv 100x100 区域,铺 4x3 拼整张;lang 中英。坑:1.21 getStateForPlacement 签名不匹配改 onPlace;误删 BlockGetter import。jar 21:43 部署
- **21:48 牌布修正:3x4 布局 + 进创造栏**:用户要求放下为 3 宽 x 4 长;材质 ffmpeg transpose=1 旋转 90°(400x300→300x400),分片 3 列 x 4 行(每片 100x100),MAT_X 0-2/MAT_Y 0-3,onPlace floorMod(x,3)/floorMod(z,4);12 个模型重建(命名 x0-2,y0-3);ModItems 创造栏加 CARD_MAT(第 5 个物品)。坑:ffmpeg 不能原地转(同文件输入输出报错),需临时文件再 Move。jar 21:48:33 部署
- **22:00 牌布改'一键铺 4x3 整张'**:不再 12 个拼。CardMatEvents(PlayerInteractEvent.RightClickItem 拦截,NeoForge 1.21 无 ItemUseEvent)右键牌布自动铺 4x3(12 片,分片按相对位置 dx/dz),检查区域可放+消耗物品+羊毛音效;CardMatBlock.onDestroyedByPlayer 挖一片整张一起消失(其他片 removeBlock 不掉落,本片 loot 掉 1 个);材质转回 400x300(4 列 x 3 行),MAT_X 0-3/MAT_Y 0-2,12 模型重建。坑:1.21 BlockItem.useOn/place 签名均不匹配无法 override,用事件;PlayerInteractEvent.RightClickItem 无 getContext,用 getPos()+getFace();Block 需 import Player/FluidState。jar 22:00:03 部署
- **22:19 牌布贴图修复(独立分片纹理)**:'全材质展示'=每格显示整张 400x300(非 2 的幂,MC 纹理加载缩放导致 uv 分片失效)。修复:System.Drawing 把整图切 12 片(crop 100x100 + 缩放 128x128,2 的幂),textures/block/card_mat_XY.png 独立纹理,模型引用各自纹理不用 uv。坑:PS 变量跨命令持久( 被污染成数组,换 /)。jar 22:19:46 部署
- **22:50 牌布朝向跟随玩家**:CardMatEvents 用 player.getDirection() 决定布局——布的纵深(3 格)朝玩家面朝方向,横向(4 格)为右手方向(getClockWise),分片 x=右手 0-3/y=前方 0-2。jar 22:50:37 部署
- **22:52 牌布换新贴图**:用户提供新图(QQ 图片,400x300 真 PNG),System.Drawing 切 12 片(crop 100x100+scale 128x128)覆盖 card_mat_XY.png,编译部署。jar 22:52:24
- **23:29 牌布图案方向反转 180°**:用户实测各朝向图案仍反,按建议 blockstate y 旋转全部 +180(north=180/east=270/south=0/west=90)。jar 23:29:53
- **23:33 牌布图案方向数学修正**:推导确认 MC up face 纹理顶(v 小)固定朝北。正解:mat_y=2-dz(近端=玩家脚边显示纹理底行,远端显示顶行),mat_x=dx,y 旋转=朝向角(0/90/180/270)。之前 mat_y=dz 导致上下颠倒,+180 是错误补偿。jar 23:33:46
- **23:56 牌布展开限制 + R键方向确认**:① R 键放置方向:用户实测确认去掉 +180 正确(改回 rot=getYRot(),此前 +180 是 14:30 加的,用户实测显示反向)。② 牌布区域不全(4x3 铺不开)时禁止放置:setCanceled(true)+actionbar 提示'空间不足,无法铺开牌布',不消耗物品。jar 23:56:51
- **00:04 牌布上方牌面统计显示(CardMatScanner)**:新增 `item/CardMatScanner.java`(@EventBusSubscriber Bus.GAME):服务端每 10 tick(0.5s)扫描在线玩家周围 24 格(垂直 ±4)的牌布分片,由分片反推整张布起点(matStart 与 onDestroyedByPlayer 同逻辑)→ 校验 4x3 完整(isMatComplete)→ AABB(布面 +0.02~+1.2)查 CardEntity → TreeMap 汇总牌名计数("杀×2 闪×1";武将牌取 '武将:' 前缀后的名字)→ 在牌布中心上方 0.6 格用**常驻 TextDisplay** 显示(亮黄 0xFFFFE040 加粗+阴影+半透明黑底 0x66000000,billboard=center);文字不变则跳过(LAST_TEXT 缓存防重建闪烁);牌被拿光/牌布被挖/玩家远离 → 移除显示。坑:1.21.1 TextDisplay.setText/getText 均 **private**(javap 验证),更新文字只能 discard 旧实体+新建(NBT 创建);EntityType.TEXT_DISPLAY 构造用 ClientHudText.HudTextDisplay 子类(NBT loadData)。已验证(编译过、jar 内类+字节码 5 项全 PASS,jar 00:04:26 部署)
- **00:25 牌方向修复(南北反)+ 牌布改剪刀挖掘**:① 用户实测"面朝南/北牌方向反,东西正常"——根因:渲染绕 Y 旋转(逆时针)与 MC yRot(顺时针,0=南)零位不一致,牌纹理顶初始朝北。正解 `rot = 180.0F - player.getYRot()`:四方向验证 东西(90/270)不变、南北(0/180)转 180,与实测吻合。改 2 处:ServerPayloadHandler.placeSelectedCard + CardItem.use(拆牌掉出的 -yRot 是显示用不动)。② 用户要牌布"剪刀快、手撸慢"(像羊毛):硬度 0.2F→0.8F(羊毛级)+ 新建 `data/minecraft/tags/block/mineable/shears.json`(replace=false,含 sanguosha:card_mat)→ 剪刀 5 倍速、手撸 1.2s。坑:修改 minecraft 命名空间标签要用 replace=false 避免覆盖原版。已验证(源码+字节码 9 项全 PASS,jar 00:25:20 部署)
- **00:35 牌布统计漏检左下角修复(AABB 边界 bug)**:用户实测"放在牌布左下角的牌不显示"。根因:`updateMat` 的 AABB 用 `start + right*4 + dir*3` 作为对角,但布实际只到 `start + right*3 + dir*2`(4x3 分片)。东/南朝向时多出的一格在远端、恰好吸收牌实体中心 `+0.5` 的溢出 → 正常;北/西朝向时多出格仍在远端,**近端行/列(坐标为 start 整数边界)的牌 bbox 与 AABB 不相交 → 漏检**。玩家面朝北铺布时近端左=左下角。修复:按布实际范围 `start~start+right*3+dir*2` 算 min/max,四方向再统一外扩 0.8(格中心偏移 0.5 + 牌 bbox 半宽 0.275)。教训:实体中心在格中心(+0.5),AABB 用方块整数坐标边界时必须在**所有方向**留出 0.5+半宽的余量,不能只靠"多算一格"碰运气。已验证(四朝向 12 格全覆盖数学模型 + 字节码 + 编译 13 项全 PASS,证据 build/hermes-verify-evidence-aabb.txt,jar 00:35:58 部署)
- **00:48 四连修:文字残留/高度/粒子紫黑/手持模型**:① **重进文字残留 bug**:TextDisplay 是实体会被存档,重进后从存档加载但 ACTIVE map(内存)为空 → 永久残留,挖布也不消失。修复三层:`HudTextDisplay` 加 `matStart` 字段+NBT 持久化(MatStartX/Y/Z)+ tick 每 40 tick(2s)自检 `CardMatScanner.isMatComplete`,牌布没了自毁;`updateDisplay` 创建前按位置(±3 AABB)清世界残留实体防叠加;`removeMat(level,start)` 改为带 level 清世界;`CardMatBlock.onDestroyedByPlayer` 挖布立即清理(不等自检)。② **文字提高 2 格**:悬浮高度 0.6→2.6。③ **破坏粒子紫黑块**:根因=12 个 block 模型缺 `particle` 字段(与 13:48 牌盒同款坑),批量正则给 `card_mat_XY.json` 加 `"particle":{"texture":"mat"}`。④ **手持看不见**:`item/card_mat.json` 原来 parent 指向 1px 薄片分片模型,拿手上不可见;改成 `cube_all` 完整方块+card_mat_00 纹理。已验证(源码断言+jar 打包+编译 18 项全 PASS,证据 build/hermes-verify-evidence-4fixes.txt,jar 00:48:31 部署)
- **00:57 文字与牌布真正"绑定"(三重清理)**:用户反馈上版(00:48)修复后文字**仍不消失**。根因分析:① `onDestroyedByPlayer` 只覆盖"玩家挖掘",爆炸/命令/替换等路径不触发;② **重进后旧 TextDisplay 是"孤儿"**——旧版本存档的实体无 matStart NBT(或牌布已被挖),`ACTIVE` 内存 map 为空清不到,而 tick 自检要求 `matStart != null` 才跑 → 永远残留。修复三层:① `CardMatBlock.onRemove` 重写——**任何方式**移除方块(玩家挖/爆炸/命令/替换,`!newState.is(this)` 判定)都立即 `removeMat` 清理,这是真正的"绑定";② `CardMatScanner.sweepOrphans` 每 10 tick 扫描玩家周围 ±24 格内**所有** HudTextDisplay,`matStart==null`(旧存档孤儿)或 `isMatComplete=false`(牌布已没)一律 discard;③ `HudTextDisplay` 自检间隔 40→20 tick 兜底。经验:**TextDisplay 实体会被存档,内存 map 管不住重进残留,必须实体自检或方块 onRemove 绑定**。已验证(源码断言+字节码+编译 20 项全 PASS,证据 build/hermes-verify-evidence-binding.txt,jar 00:57:04 部署)
- **01:06 重进残留真正根因:实体类型存档后丢失 + CustomName 标记方案**:用户反馈上版(00:57)仍残留且放牌出现**重叠文字**、挖掉后旧的还在。**根本根因:TextDisplay 实体存档重进后,MC 按 EntityType 反序列化生成"原版 TextDisplay",不是我们的 HudTextDisplay 子类**——子类字段(matStart)/tick 自检全丢,且 `getEntitiesOfClass(HudTextDisplay.class)` 匹配不到原版实体 → 旧文字永久残留、清理无效、新旧叠加。修复:改用 **CustomName 标记**识别(`§§sgsmat:x,y,z`,原版 TextDisplay 也会持久化 CustomName):`ClientHudText` 加 `markerFor`/`parseMarker` 静态方法;创建统计文字时 `setCustomName(marker)+setCustomNameVisible(false)`;所有清理(`updateDisplay`/`removeMat`/`sweepOrphans`)改扫**父类 `Display.TextDisplay`** 按标记匹配 start;无标记旧实体兜底:updateDisplay 按位置重叠(±1.5/±1.0)清、removeMat 按高度(布面+1.8~3.4)清、sweepOrphans 用 `hasAnchorBelow`(下方有牌布/牌盒则保留,防误杀摸牌临时文字)。**教训:自定义实体子类存档后类型丢失,持久化身份必须用 CustomName/UUID 等原版字段,不能依赖子类类型或子类字段**。已验证(源码断言+字节码+编译 22 项全 PASS,证据 build/hermes-verify-evidence-marker.txt,jar 01:06:26 部署)
- **01:11 退出世界崩溃修复(ConcurrentModificationException)**:用户退出世界时崩溃,crash-reports/crash-2026-08-04_01.09.46-server.txt。日志定位:`java.util.ConcurrentModificationException at HashMap$HashIterator.remove(HashMap.java:1619) at CardMatScanner.onServerTick(CardMatScanner.java:65)`。**根因:迭代器遍历 `ACTIVE` map 时,循环体内先调 `removeMat()`(内部执行 `ACTIVE.remove(key)` 修改 modCount),接着 `it.remove()` → HashMap 迭代器校验 modCount 失败 → CME**。退出世界时服务端收尾 tick 恰好触发。修复:改为**先收集 stale key 列表,循环外统一 removeMat**(不再迭代中改 map)。教训:遍历 HashMap 时若循环体可能修改同一 map(哪怕经方法间接),必须收集后删除或用 ConcurrentHashMap。已验证(源码断言+字节码无 HashIterator.remove+编译 16 项全 PASS,证据 build/hermes-verify-evidence-cme-fix.txt,jar 01:10:59 部署)。另注:日志仍见 `Display entityNot a string` ERROR(17:07 已知,TextDisplay 旧 NBT 加载告警,不影响运行,待观察)
- **01:21 牌盒文字残留修复(临时文字标记,照牌布思路)**:用户要求"照牌布思路修牌盒文本相同 bug"。**根因:牌盒摸牌/发将提示文字(CardBoxBlock useWithoutItem 调 `ClientHudText.show()`)创建的 TextDisplay 无 CustomName 标记**——重进后 STANDS(内存 map)清空、实体变回原版 TextDisplay → 永久残留;且 sweepOrphans 的 hasAnchorBelow 见下方有牌盒反而"保护"残留。修复:① `ClientHudText` 加 `TMP_MARKER_PREFIX=§§sgstmp:` + `tmpMarkerFor(UUID)`/`isTmpMarker`/`isTmpTracked`(查 STANDS 是否跟踪中);② `show()` 创建时 `setCustomName(tmpMarkerFor(randomUUID))+setCustomNameVisible(false)`;③ `sweepOrphans` 新分支:isTmpMarker 且 `!isTmpTracked`(重进后 STANDS 空)→ discard,正常游戏中 STANDS 跟踪中 → 保留。**要点:临时文字靠"是否在 STANDS 跟踪中"区分正常显示 vs 重进残留,重进后 STANDS 是空的 → 自动判定残留**。已验证(源码断言+字节码+编译 18 项全 PASS,证据 build/hermes-verify-evidence-tmpmarker.txt,jar 01:21:42 部署)
- **01:32 牌布物品模型改地毯薄片 + 破坏粒子紫黑修复**:① 物品模型:用户反馈 cube_all 完整方块不像地毯。改成**自包含薄片元素(16x2x16)+ display 变换**(gui 旋转 30/45 倾斜、手持/地面缩放,参考 deck_box_block.json 的 display),textures 用 card_mat_00;这样物品栏/手持显示为扁片地毯。② **破坏粒子紫黑块**:12 个 block 模型之前 particle 是顶层对象写法 `"particle": {"texture": "mat"}`,而牌盒(13:48 已验证破坏粒子正常)用的是 **textures 对象内键** `"particle": "sanguosha:block/box_top"`。全部改为与牌盒一致:`"textures": {"mat": "...card_mat_XY", "particle": "sanguosha:block/card_mat_XY"}`(每个模型引用自己的贴图,非全 00)。教训:1.21 破坏粒子取模型 textures.particle 键,顶层对象写法可能不生效,统一用牌盒已验证格式。已验证(12 模型 particle 一致性+物品薄片+jar 打包 10 项全 PASS,证据 build/hermes-verify-evidence-matmodel.txt,jar 01:32:03 部署)
- **01:51 牌盒上级界面 + 诸葛亮观星(线下牌堆)**:用户要求"牌盒 UI 加上级界面,让观星可用;先不急着 2v2,就在牌堆里"。实现:① **上级界面 BoxMenuScreen**(新):蹲下右键牌盒 → 服务端 sendRemain(现有)→ 客户端 handleRemain 改打开 BoxMenuScreen(标题+「查看剩余」/「观星(诸葛亮)」/「返回游戏」按钮,将盒无观星按钮);「查看剩余」→ RemainBoxScreen(现有)。② **观星全链路**:新 `GuanXingSyncPacket`(S2C,pos+names"牌名|花色|点数")注册 Networking;ActionPacket 加 GUANXING_VIEW/GUANXING_CONFIRM;`LocalCardDeck` 加 `peekTop(n)`(看顶 n 张不取出)+ `reorderTop(n, order)`(按 order 逆序插回顶部,未列入的放堆底);ServerPayloadHandler 加 guanXingView(peekTop(5) 发包)/guanXingConfirm(解析 "x,y,z,deck|idx,..." 调 reorderTop);③ **观星界面 GuanXingScreen**(新):显示牌堆顶 5 张,每张可 ▲▼ 调序、↓底/↑顶 切换,确认后发 GUANXING_CONFIRM(编码顶部顺序下标,未列出的放底),ESC 取消。观星数固定 5(牌堆不足全看)。已验证(源码断言+jar 打包+编译 28 项全 PASS,证据 build/hermes-verify-evidence-guanxing.txt,jar 01:50:50 部署)
- **02:00 观星牌数选择子界面(GuanXingCountScreen)**:用户要求"观星后再加一个子界面,选择观星牌数"(对应诸葛亮观星 X 张规则,线下 1~5)。新增 `GuanXingCountScreen`(客户端):上级界面点「观星」→ 打开数量选择(1~5 张按钮,ESC 取消)→ 选 n 发 GUANXING_VIEW 编码 `"x,y,z,deck|n"` → 服务端 guanXingView 解析 count(clamp 1..5)后 `peekTop(count)` → GuanXingScreen 按 names.size() 动态渲染任意数量(原已支持)。确认编码升级为 `"x,y,z,deck|count|idx,..."`,guanXingConfirm 解析 count 后 `reorderTop(count, order)`(原来硬编码 5)。已验证(源码断言+字节码+jar+编译 18 项全 PASS,证据 build/hermes-verify-evidence-guanxingcount.txt,jar 02:00:06 部署)
- **02:23 中文化 + 合成配方**:① en_us.json 全部改为中文(含补全 key.sanguosha.open_table/drop_card/clear_cards、item.card/deck_box/hero_deck_box/card_mat),zh_cn.json 修复**缺逗号导致的 JSON 语法错误**(第14行 hero_deck_box 后无逗号 → 语言文件解析失败、游戏回退英文——这是"全是英文名"的根因);② 新增 3 个合成配方 data/sanguosha/recipe/:**deck_box.json**(1 任意木板 + 1 任意木台阶,产物 sanguosha:deck_box)、**hero_deck_box.json**(2 木板 + 2 木台阶,产物 hero_deck_box)、**card_mat.json**(2 任意地毯 tag wool_carpets,产物 card_mat);③ neoforge.mods.toml displayName 写死"三国杀"+中文描述(gradle.properties 是 ISO-8859-1 编码,写中文会乱码,故直接在 toml 写死;build.gradle expand 只替换 ${} 占位符,写死文本不受影响)。已验证:jar 内 3 配方 + 2 lang(UTF-8 中文)+ toml displayName,jar 02:23:18 部署 27MB

- **02:35 配方合成失败修复(shapeless)**:用户反馈"牌布能合成、牌盒/将盒合成不了"。诊断:① 日志 `Loaded 1293 recipes` = 原版 extra.jar 1290 + 我们 3 个,**配方全部加载成功**,无解析错误;② 1.21.1 原版确认存在 `planks`/`wooden_slabs`/`wool_carpets` 三个物品 tag(client-extra.jar 内 data/minecraft/tags/item/)。**根因:`crafting_shaped` 精确匹配 pattern 且不旋转**——牌布 `["CC"]` 两地毯对称怎么摆都能成;牌盒 `["PS"]` 只认"左木板右台阶"、将盒 `["PP","SS"]` 只认"上两木板下两台阶",用户手动摆放方向一错就合不出。修复:三个配方全部改为 `minecraft:crafting_shapeless`(无序合成,材料对即可,任意摆放都能合);曾先加 3 个方向变体(_vertical)后删除(shapeless 已全覆盖,变体冗余会让配方书出现重复条目)。已验证:jar 内 3 配方均 crafting_shapeless,jar 02:34:58 部署。教训:玩家手动合成是黑盒,方向敏感的形状配方用 shapeless 最稳;牌布能合≠配方方向正确(对称巧合)

- **12:24 右键放置卡牌禁用**:用户反馈右键放牌会放到牌布(1px 薄片)下面。用户在 VS Code 已自行改好 `CardItem.use`(直接 `InteractionResultHolder.pass`,注释写明原因)——本会话确认:① 摸牌/翻转/捡起等实体交互走 `CardPlaceEvents.onRightClickEntity`(EntityInteract 事件),与 CardItem.use 无关,不受影响;② R 键放置走 `SanguoshaHud` → ActionPacket → `ServerPayloadHandler.placeSelectedCard`,也不受影响。构建注意:`gradlew build` 显示 32 up-to-date 但用户改的源码未重新编译——需 `compileJava --rerun-tasks` 强制重编(class 时间戳 12:24:40),再 build 打包。已验证:jar 内 CardItem.class 字节码 use() 仅 pass、无 addFreshEntity/shrink,jar 11:48:34 部署

- **12:46 发布前代码清理**:① 删除孤立类 `LobbyScreen.java`(2v2 大厅已禁用,全代码 0 引用,仅剩 import 残留);② 清理 9 处未使用 import:SanguoshaClientEvents(InputConstants/GLFW/RegisterKeyMappingsEvent/LobbyScreen/HeroSelectScreen/SanguoshaScreen/KeyMapping 等,注意 HeroSelectScreen/SanguoshaScreen 误删后已恢复)、BoxMenuScreen(RemainSyncPacket/ArrayList/PacketDistributor)、ChoiceScreen(ClientGameState)、ModBlocks(ModItems)、SanguoshaGame(CardCategory)、CardData(RecordCodecBuilder/StreamCodec)、CardMatScanner(Iterator,CME 修复后遗留)、SanguoshaHud(RenderGuiEvent)、ModKeybinds(InputConstants);③ README.md 从 MDK 模板重写为模组中文介绍(功能/配方表/安装/开发/许可);④ 删除 TEMPLATE_LICENSE.txt;⑤ gradle.properties 的 mod_name/mod_description 改为占位(显示名已写死在 mods.toml 中文)。编译 13 警告=已知 NeoForge 过时 API(bus()/initializeClient,进度.md 已记录不影响运行,涉及 MOD 总线语义未动)。已验证:jar 无 LobbyScreen.class、关键类完好、jar 12:46:35 部署

- **13:04 牌布统计误检修复(布外一格也计入)**:用户反馈"牌放到牌布前面一格还是会被检测到"。根因:`CardMatScanner.updateMat` 的 AABB 四方向**外扩 0.8**——牌 bbox 半宽 0.275、中心在格中心(+0.5),布内边缘格的牌 bbox 内缘距布边缘仅 0.225,而**布外前一格的牌 bbox 从 E+0.225 起**,外扩 0.8 后两者都被包进 AABB。修复:**去掉 0.8 外扩,直接用布精确范围**(start~start+right*3+dir*2)。数学验证:AABB 是相交检测非包含——布内边缘牌 bbox 内缘 0.225<布边缘 → 仍相交(不漏检);布外牌 bbox 从 E+1.225 起 > 布边缘 → 不相交(不误检)。已验证:CardMatScanner.class 常量池无 0.8f(CD-CC-CC-3F 不在),jar 13:04:01 部署(1.3.0 覆盖)。教训:上一版 00:35 为修漏检把 AABB 四向扩 0.8,扩过头导致布外一格误检——AABB 相交检测下精确范围即可,外扩是多余的

- **13:25 手持物品蹲下右键牌盒打不开修复**:用户反馈"手里拿着东西时蹲下右键打不开牌盒"。根因:`CardBoxBlock` 只重写了 `useWithoutItem`(空手右键),MC 1.21 交互链中**手持物品右键方块先走 item.useOn**——手持木板等 BlockItem 会直接尝试放置(返回 SUCCESS 吞掉方块交互),`useWithoutItem` 永不触发。修复:新建 `CardBoxInteractEvents`(@EventBusSubscriber 同包类),监听 `PlayerInteractEvent.RightClickBlock`,目标方块是 `CardBoxBlock` 时 `setCanceled(true)` 并直接调 `box.useWithoutItem(...)`(同包可访问 protected),在物品放置**之前**强制走牌盒逻辑;开头加 `event.isCanceled()` 防御(与手持牌布铺布逻辑互不干扰)。已在 SanguoshaMod 构造器注册。效果:无论手持什么(木板/卡牌/工具),蹲下右键=查看剩余 UI,非蹲下=摸牌/发将,不再被物品放置抢占。已验证:jar 含 CardBoxInteractEvents.class + SanguoshaMod 注册行,jar 13:25:13 部署

- **16:50 HUD 模式 Q 键拦截 + V 清除确认界面**:① HUD 打开时禁用原版 Q 丢物品——`onClientTickPre` 在 Minecraft.handleKeybinds 前抢先 `consumeClick(mc.options.keyDrop)`(与数字键同机制);模组 DROP_CARD(丢出选中牌)用 isDown() 检测不受影响。② V 键(CLEAR_CARDS)不再直接发包,改为 `mc.setScreen(new ClearConfirmScreen())`(新类,红"确认清除"/"取消"两按钮,确认后才发 CLEAR_CARDS,ESC/取消关闭,isPauseScreen=false;加 mc.screen==null 防重复弹窗)。已验证:字节码 keyDrop+consumeClick、ClearConfirmScreen 构造,jar 16:50:58 部署(1.3.0)

- **12:46 禁用右键放置卡牌(CardItem.use)**:用户反馈右键放置卡牌会放到牌布下面。**根因**:右键放置走 `CardItem.use()` 的 `player.pick(5.0,...)` 命中牌布(1px 薄片方块)后,把实体牌放到 `Math.floor(loc.y)+0.02`——牌布只有 2px 高,实体牌中心落点被算在牌布方块格内,牌卡进/掉到牌布下面。**修复**:`CardItem.use` 改为直接 `return InteractionResultHolder.pass(...)`(不再放置);R 键放置(PLACE_CARD → 网络包 → ServerPayloadHandler.placeSelectedCard)不受影响,摸牌/翻转/捡起(CardPlaceEvents EntityInteract 事件)不受影响。清理未用 import(HitResult/Vec3/ThreadLocalRandom)。已验证:源码无 addFreshEntity/shrink/pick、字节码 use() 仅 pass 一行、jar 与部署字节一致(27250017)。教训:薄片类方块(1px)上不能靠 floor(y)+偏移放置实体牌,统一走 R 键放置逻辑

- **2026-08-07 新环境迁移 + 大版本推进(1.3.0,跨对话接力)**:
  - 项目自 GitHub 克隆(BigDare819/MCsanguosha)到 C:\Users\Administrator\dev\MCsanguosha;git 身份占位 userName/userEmail(commit 936999e);git 全局代理已配 127.0.0.1:7890
  - **PCL 真相**:启动器本体 Downloads\PCL,真实游戏目录 **C:\Users\Administrator\Desktop\.minecraft**(版本隔离,实例独立在 versions\<名>\);「三国杀」= NeoForge 1.21.1 完整实例(mainClass=BootstrapLauncher,含 iris/sodium/CustomSkinLoader);部署目标 versions\三国杀\mods\sanguosha-1.3.0.jar
  - **新增 25 名界武将**(界华佗+界曹操...界诸葛亮,纯展示不参与 2v2):Heroes.java add + CardModelIds.HERO_KEYS(61 个) + 模型 card/81~105.json + card.json override 105 个 + hero/item 贴图各 25(1095x1539 原图无损,jar 183MB);将盒拼音搜索 jie 前缀可用
  - **代码优化**:① Heroes.nameToId 界前缀规则化——以"界"开头先去"界"查基础武将再拼 jie 前缀,删 24 个硬编码 case(界徐盛 jiexusheng 保留显式,因"徐盛"无基础映射);② PinyinUtil 删冗余 HERO_PINYIN map(30 条,nameToId fallback 已全覆盖)。验证:javac+java 运行时 17 项 + gradle build
  - **一键清空弃牌区**:DiscardRecordScreen 底部加「一键清空」按钮 → 新 ActionPacket.DISCARD_CLEAR → ServerPayloadHandler.discardClear(删除 4x4 弃牌布上方 CardEntity/卡牌 ItemEntity + DiscardDeckManager.clear(pos) 清记录 + discardView 刷新 UI + DiscardMatScanner.refreshDisplay 移除浮空文本);DiscardDeckManager 新增 clear()
  - **构建坑**:gradle.properties 只有 nonProxyHosts 没配 proxyHost → wrapper 直连 services.gradle.org 被墙 SSL 失败;解决:构建前 `$env:JAVA_TOOL_OPTIONS="-Dhttp.proxyHost=127.0.0.1 -Dhttp.proxyPort=7890 -Dhttps.proxyHost=127.0.0.1 -Dhttps.proxyPort=7890"`(Mojang 资源经 nonProxyHosts 直连)
  - **分发位置**:Videos\Captures\方块杀(最新版 jar)+ 方块杀旧版(旧版归档,用户管理)+ Downloads\MCsanguosha-1.3.0-modpack.zip(CurseForge 格式整合包,PCL2 导入自动装 MC1.21.1+NeoForge21.1.234)
  - **待办**:git push 需 GitHub 认证(GCM 弹窗,尚未完成);过时 API 警告(EventBusSubscriber bus()/initializeClient,约 6~15 处)待清理;patch 工具模糊匹配曾误删 card.json 79/神赵云等 case,改文件后务必核对

- **2026-08-07 第二轮(本会话,跨对话接力点)**:弃牌区三连改 + 新武将 2 名 + 备份:
  - **弃牌记录 UI 搜索栏**(DiscardRecordScreen,照 RemainBoxScreen 模式):EditBox 搜索框(顶部,提示"搜索(拼音/中文)",init() 创建防 font null)+ visibleIndexes() 过滤(中文 contains + PinyinUtil 拼音 contains,返回原始索引)+ 倒序显示映射 `vis.get(vis.size()-1-vi)` + 点击拿取发真实记录索引 + 搜索词变化重置滚动(lastQuery 字段)+ 空态区分"无匹配结果"/"还没有弃牌记录"。已验证 13 项
  - **弃牌布字幕淡出**(用户:字幕挡玩家对视,要求出现 4 秒后逐渐消失):`ClientHudText.HudTextDisplay` 加 fadeOut 生命周期(可选开关,牌布统计文字不开启保持常驻)——lifetime≥80 tick(4s)后每 tick 重载 NBT `text_opacity` 255→0 线性下降(20 tick=1s 平滑渐隐),≥100 tick discard;`DiscardMatScanner.updateDisplay` 开启 setFadeOut+setBaseTag(保存完整 NBT);onServerTick 清理 ACTIVE 残留(LAST_TEXT 保留→无新弃牌不自动重建,只有新弃牌/内容变化才重新显示)。**坑:1.21.1 `TextDisplay.setTextOpacity`/`startInterpolation`/`DATA_TEXT_OPACITY_ID` 全是 private(javap 验证),淡出只能每 tick loadData 重载 NBT 改 text_opacity**。已验证 11 项
  - **弃牌区武将牌显示拼音修复**:`ServerPayloadHandler.discardView` 下发 names 时对武将牌(`武将:id|名字`)取 parts[0]→显示 `武将:caocao` 拼音 id。修复:武将牌取 parts[1] 汉字名(与 CardMatScanner/DiscardMatScanner.formatLine 一致);`discardTake` 的 name 同样取 parts[1](物品名/提示消息)。汉字名经 `PinyinUtil.toPinyin`→`Heroes.nameToId` fallback 自动可拼音搜索。已验证 12 项
  - **新武将①界甘宁**(用户先发"锦帆游侠·甘宁"图后又更正为界甘宁图):名字`\u754c\u7518\u5b81`,id `jieganning`,**nameToId 无需显式 case**(界前缀规则:`界甘宁`→去界→`ganning`→`jieganning`),序号 106(45+61);替换流程=删旧 case+add 行改名字+HERO_KEYS 改 id+模型 layer0 改+贴图替换,无残留引用
  - **新武将②big**(用户:big 就是她的名字,拉丁字母武将):名字/id 均 `big`,nameToId 走 default 原样返回(无需 case),序号 107;拼音/英文搜索 `big`/`bi` 大小写均可命中。贴图 big.png(1098x1542)字节级复制
  - **武将身份识别技巧**:图名无武将信息时用 **Windows OCR**(PowerShell WinRT `Windows.Media.Ocr.OcrEngine`,zh-Hans 语言包,AsTask 包装异步)读武将牌技能文字/台词;名字区艺术字 OCR 常失败,可 System.Drawing 裁切+放大+二值化增强,仍失败就问用户
  - **备份**:最新 jar 复制为 `Videos\Captures\方块杀\sanguosha1.4.0.jar`(仅命名备份,gradle 版本号仍 1.3.0)
  - **当前武将总数 63**:HERO_KEYS 45..107(含 jieganning/big);贴图源目录 `Pictures\武将\`(界甘宁.png、big.png)
